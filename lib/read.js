(function () {
    'use strict';

    // Vars
    var XP = require('expandjs'),
        zlib = require('zlib');

    /*********************************************************************/

    module.exports = function (data, cb) {
        cb(null, data);
    };

}());
